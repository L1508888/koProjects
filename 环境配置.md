

make distclean

export CLANG_PATH=/home/liuxin/pixelKernel/prebuilts/clang/host/linux-x86/clang-r450784e
export PATH=${CLANG_PATH}/bin:$PATH
 
make ARCH=arm64 CC=clang LLVM=1 LLVM_IAS=1 defconfig

make ARCH=arm64 CC=clang LLVM=1 LLVM_IAS=1 prepare


make ARCH=arm64 CC=clang LLVM=1 LLVM_IAS=1 modules_prepare





make mrproper

BUILD_CONFIG=common/build.config.gki.aarch64 build/build.sh



fastboot flash --slot all boot boot.img


cd /home/liuxin/pixelKernel/common

# 删除所有编译生成的文件
find . -name '*.o' -delete
find . -name '.*.cmd' -delete          # 隐藏的 .<filename>.cmd 文件
find . -name '*.mod.c' -delete
find . -name '*.ko' -delete
find . -name '*.mod' -delete
find . -name '*.symvers' -delete
find . -name '.tmp_versions' -type d -exec rm -rf {} +

# 删除常规 mrproper 应该清理的目录和文件
rm -rf include/config include/generated .version .config.old
rm -rf arch/*/include/generated
rm -f modules.order Module.markers

# 如果之前用 git 管理，可以安全清理未跟踪文件（注意保存你的修改）
git clean -fd   # 会删除所有未加入版本控制的文件，请先备份好你的 export 修改！


## 内核源码获取

~~~
repo init -u https://android.googlesource.com/kernel/manifest -b android-gs-raviole-5.10-android13-qpr3
repo sync -j$(nproc)
~~~


## 编译内核
~~~
BUILD_CONFIG=private/gs-google/build.config.slider build/build.sh

~~~



fastboot getvar version-bootloader

fastboot set_active a
fastboot set_active b